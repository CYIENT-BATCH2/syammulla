H1>ALGORITHM:CONDITIONAL OPERATOR
	1)According to their requirements we have to find biggest number using conditional operator.
	2)if x value is greater than y,print y value is bigger else x value is bigger.

H2>ALGORITHM:CONDITIONAL OPERATOR
	1) According to their requirements we have to find even or odd using conditional operator.
	2)if num&1 is equality of zero means print it is a even number else print the odd number.

H3>ALGORITHM:CONDITIONAL OPERATOR
	1)According to their requirements we have to find even or odd using conditional operator.
	2)if num is greater than zero means print it is a positive number,else print the negative number.
